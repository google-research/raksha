module m0 {
  block b0 {
        %0 = core.input [name: "revenue_table"] ()  
        %epsilon = global_epsilon [epsilon: "2"] ()
        %1 = sql.filter [column: "revenue"] (%0.out) // { revenue > 100 }
        %location = sql.column_access [column: "location"] (%1.out)
        %2 = sql.group_by[] (%1.out, %location.out) // (%0, location)
        %user = sql.column_access [column: "user"] (%2.out)
        %address = sql.column_access [column: "address"] (%2.out)
        %4 = sql.output [] (%user.out) // Select user, address from …
        %5 = sql.output [] (%address.out)
    }
}
